# State — [Nome da Spec]

> Resumo agregado de quais tasks desta spec estão pendentes e quais estão
> bloqueadas, para retomar a execução após uma interrupção. O detalhe de
> cada task mora no próprio arquivo `tasks/[TASK-CODE].md` (campo `status`
> no frontmatter: `pending` | `in_progress` | `completed`); este arquivo é
> só o índice agregado — atualize os dois juntos. Tasks `completed` saem
> das listas abaixo.

## PENDING TASKS:
- [TASK-CODE] — [título curto, opcional]
...

## BLOCKED TASKS:
- [TASK-CODE] — [título curto, opcional]: [motivo do bloqueio e o que
  precisa acontecer para destravar; registrar a issue relacionada se houver
  uma, ver skill `issue-generator`]
....