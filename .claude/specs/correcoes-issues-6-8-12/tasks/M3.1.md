---
id: TASK-M3.1
title: "(design, sem código) Contrato de resposta de Adapter"
status: pending
depends_on: []
references:
  requirements: ["REQ-57.1", "REQ-57.4"]
  design: ["Secao 4.5"]
target_files:
  - ".claude/specs/correcoes-issues-6-8-12/design.md"
---

# TASK-M3.1: (design, sem código) Contrato de resposta de Adapter

## 1. Contexto & Objetivo
> **Instrução para o Claude Code:** Leia `design.md` §4.5 antes de iniciar. Esta task **não altera código nenhum** — só `design.md`.

**Sem esta decisão, REQ-57 não tem alvo: não existe tipo que `X` possa assumir.**

Verificado:

1. `Call<Nome>` é emitido como `func Call<Nome>(ctx, n <Notif>) error`
   (`codegen/decl_io.go`) — devolve só `error`. O próprio comentário de
   `emitSagaMock` já dizia "o valor não influencia o fluxo de negócio nesta fase".
2. Nenhuma seção do spec da linguagem define **tipo de resposta** para
   `Adapter`/`Notification`.
3. O `PaymentResult(...)` do exemplo §22.3 **não é declarado em lugar nenhum** do
   spec — `grep PaymentResult` acha só as linhas do próprio exemplo.

Foi o salto por cima desta camada que fez a task original do Marco L (L2.3)
nascer subdimensionada: ela descrevia "trocar o retorno do stub", quando faltavam
três camadas abaixo disso.

## 2. Passos de Implementação
- [ ] **Step 1:** Reler as seções do spec da linguagem sobre `Adapter`,
  `Notification` e §18.2 (`result = call ...`) em
  `.claude/steerings/domainscript-spec-v7/` — carregar **só** as seções
  necessárias, resolvendo a citação por **título**, não por número (ver
  `CLAUDE.md` sobre a renumeração v6→v7).
- [ ] **Step 2:** Decidir e registrar o contrato. Opções a avaliar:
  - **(a) Resposta tipada pela própria `Notification`** — o tipo de retorno sai
    da declaração que já existe, sem gramática nova. É a rota que mantém REQ-57
    dentro do escopo de back-end.
  - **(b) `Adapter X returns <Tipo>` declarado** — exige gramática nova
    (léxico→parser→resolver→sema): sai do escopo deste ciclo, mesma natureza de
    ISSUE-2.
  - **(c) Delimitar** — sem contrato, REQ-57.2/57.3 ficam fora e a fase M3
    termina aqui, documentada.
- [ ] **Step 3:** Registrar em `design.md` §4.5 e na tabela §7.2: a opção
  escolhida, o que ela habilita, e o motivo das descartadas. **Se a escolha for
  (b) ou (c), dizer explicitamente que M3.2 e M3.3 ficam canceladas** (REQ-57.4)
  e refletir isso no `state.md` da spec e em REQ-61 (delimitação documentada).

## 3. Testes & Validação
Task de design — **nenhum teste de código**. Critérios documentais:

- [ ] **TEST-1:** `design.md` §4.5 nomeia o contrato escolhido e mostra a
  assinatura Go resultante de `Call<Nome>`.
- [ ] **TEST-2:** §7.2 registra as opções descartadas com o motivo.
- [ ] **TEST-3:** a decisão diz explicitamente se M3.2/M3.3 seguem ou ficam
  delimitadas — sem ambiguidade para quem executar a próxima task.
- [ ] **TEST-4:** nenhum arquivo `.go` foi modificado.

## 4. Mensagem de Commit
docs(repo): define o contrato de resposta de Adapter
