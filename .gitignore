# Binários compilados
/dist/
/dsc
/dsc.exe
*.exe

# Artefatos de teste e cobertura
*.test
*.out
coverage.*

# Configuração local do Claude Code (pessoal, não versionada)
.claude/settings.local.json

# Worktrees isolados de subagentes em background (Claude Code) — efêmeros,
# limpos automaticamente quando o agente termina, nunca conteúdo do repo.
.claude/worktrees/

# Caches de IDE / editor
.idea/
.vscode/
*.swp
*~
.DS_Store
