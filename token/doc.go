// Package token define as unidades léxicas da linguagem DomainScript:
// TokenKind, Token, Pos (linha/coluna 1-based) e o mapa de keywords.
//
// É a base do pipeline (REQ-1); não depende de nenhum outro pacote do projeto.
package token
