---
id: TASK-M5.3
title: "Fechamento do Marco M: revisão de DoD"
status: pending
depends_on:
  - TASK-M1.6
  - TASK-M2.4
  - TASK-M3.3
  - TASK-M4.1
  - TASK-M4.2
  - TASK-M5.1
  - TASK-M5.2
references:
  requirements: ["REQ-61.3"]
  design: ["Secao 6"]
target_files:
  - "../../../state.md"
  - "../../correcoes-issues-6-8-12/state.md"
---

# TASK-M5.3: Fechamento do Marco M: revisão de DoD

## 1. Contexto & Objetivo
> **Instrução para o Claude Code:** Leia `requirements.md` §5 (Definition of Done) antes de iniciar. Esta task fecha a spec.

Conferir, item a item, que o ciclo cumpriu o DoD, e deixar os ponteiros do repo
consistentes. **Sem `go test ./...` local** — o CLAUDE.md é explícito: a suíte
completa é trabalho do CI, inclusive no fechamento; a prova é a PR verde.

Se alguma task ficou `blocked` (por decisão de design em M2.2/M3.1, ou por um
bloqueio registrado como issue), o fechamento **declara isso explicitamente** em
vez de marcar o Marco como completo por inteiro — o mesmo rigor que a §7.1 do
`design.md` aplicou às premissas.

## 2. Passos de Implementação
- [ ] **Step 1:** Percorrer `requirements.md` §5, item por item, confirmando cada
  um contra o que foi entregue — e anotando o que ficou fora, com o motivo.
- [ ] **Step 2:** Confirmar na PR da spec que o CI está verde: `go build`,
  `go vet`, `gofmt -l .` e a suíte completa, incluindo o job `examples` com o
  `pizzeria` gerando e compilando.
- [ ] **Step 3:** Confirmar que `../../../issues/` e `../../codegen/gaps.md`
  refletem o estado final (produto de M1.6 e M5.2): **ISSUE-12** `RESOLVED`;
  **ISSUE-6** resolvida na fatia fechada com os resíduos apontados; **ISSUE-8**
  fechada em (b) e reclassificada em (a)/(c).
- [ ] **Step 4:** Esvaziar `PENDING TASKS` no `state.md` desta spec, mantendo em
  `BLOCKED TASKS` apenas o que de fato ficou bloqueado, com o motivo.
- [ ] **Step 5:** Atualizar o ponteiro de spec-task em `../../../state.md`,
  **sobrescrevendo a linha** (nunca anexando): apontar para a próxima spec-task
  do repo, ou limpar se não houver. Reavaliar o ponteiro de issue — é uma
  decisão manual de prioridade.
- [ ] **Step 6:** Comentar `SPEC FINALIZADA` na PR da spec, conforme o
  `CLAUDE.md`.

## 3. Testes & Validação
Task de fechamento — a validação é o CI da PR, não execução local:

- [ ] **TEST-1:** CI verde na PR da spec, incluindo o job `examples` com
  `pizzeria`.
- [ ] **TEST-2:** cada item de `requirements.md` §5 tem um veredito explícito —
  cumprido, ou não cumprido com motivo.
- [ ] **TEST-3:** `../../../state.md` tem exatamente duas linhas de ponteiro
  (spec-task e issue), sem tabela nem histórico — a regra do `CLAUDE.md`.
- [ ] **TEST-4:** `state.md` desta spec não lista task pendente alguma que não
  esteja de fato pendente.

## 4. Mensagem de Commit
docs(repo): fecha o Marco M e atualiza os ponteiros
